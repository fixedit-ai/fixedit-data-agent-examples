
# ========================================
# From file: test_comments.conf
{{REGEX:# Git repo: .*}}
{{REGEX:# File path in repo: .*}}
{{REGEX:# Commit: [0-9a-f]{12}}}
{{REGEX:# Status: (clean|modified|staged|untracked|unknown)( \(.*\))?}}
# ========================================

# This commented reference should NOT be inlined
# [[processors.starlark]]
#   script = "scripts/hello.star"

[[inputs.cpu]]
  percpu = false
  totalcpu = true

# This real reference SHOULD be inlined
[[processors.starlark]]
  source = '''
def apply(metric):
    metric.tags["processed"] = "true"
    return metric

'''

[[outputs.file]]
  files = ["stdout"]
