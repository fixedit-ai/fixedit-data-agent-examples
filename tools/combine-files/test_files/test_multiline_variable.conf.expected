
# ========================================
# From file: test_multiline_variable.conf
{{REGEX:# Git repo: .*}}
{{REGEX:# File path in repo: .*}}
{{REGEX:# Commit: [0-9a-f]{12}}}
{{REGEX:# Status: (clean|modified|staged|untracked|unknown)( \(.*\))?}}
# ========================================

# Test that multi-line variable expansion doesn't break subsequent inlining
[agent]
  interval = "10s"
  flush_interval = "10s"
  # This variable expands to a multi-line string value
  description = ${MULTILINE_DESC}

[[inputs.cpu]]
  percpu = false
  totalcpu = true

[[processors.starlark]]
  source = '''
def apply(metric):
    metric.tags["processed"] = "true"
    return metric

'''

[[outputs.file]]
  files = ["stdout"]

