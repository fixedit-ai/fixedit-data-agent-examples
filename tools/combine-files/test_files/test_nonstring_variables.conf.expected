
# ========================================
# From file: test_nonstring_variables.conf
{{REGEX:# Git repo: .*}}
{{REGEX:# File path in repo: .*}}
{{REGEX:# Commit: [0-9a-f]{12}}}
{{REGEX:# Status: (clean|modified|staged|untracked|unknown)( \(.*\))?}}
# ========================================

# Test config with variables in non-string fields (like booleans)
# This should not cause errors even though debug = ${TELEGRAF_DEBUG} is invalid TOML

[agent]
  # This is a boolean variable, so it needs to be expanded to
  # make valid TOML.
  debug = ${TELEGRAF_DEBUG}

[[inputs.cpu]]
  percpu = false
  totalcpu = true

[[processors.starlark]]
  source = '''
def apply(metric):
    metric.tags["processed"] = "true"
    return metric

'''

[[outputs.file]]
  files = ["stdout"]

